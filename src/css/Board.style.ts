import * as CSS from "csstype";

const emptyBoard: CSS.Properties = {
  zIndex: -1,
};

const newGameBtn: CSS.Properties = {
  position: "absolute",
  top: "290px",
};

export default { emptyBoard, newGameBtn };
